<h1>Listing admin_users</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Password</th>
      <th>Password confirmation</th>
      <th>Name</th>
      <th>Nick</th>
      <th>Birth</th>
      <th>Address</th>
      <th>Phone</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admin_users.each do |admin_user| %>
      <tr>
        <td><%= admin_user.email %></td>
        <td><%= admin_user.password %></td>
        <td><%= admin_user.password_confirmation %></td>
        <td><%= admin_user.name %></td>
        <td><%= admin_user.nick %></td>
        <td><%= admin_user.birth %></td>
        <td><%= admin_user.address %></td>
        <td><%= admin_user.phone %></td>
        <td><%= link_to 'Show', admin_user %></td>
        <td><%= link_to 'Edit', edit_admin_user_path(admin_user) %></td>
        <td><%= link_to 'Destroy', admin_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_admin_user_path %>
